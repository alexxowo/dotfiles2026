# ==============================================================================
# TMUX CONFIGURATION - CATPPUCCIN MOCHA THEME
# ==============================================================================

# --- General Settings ---------------------------------------------------------
# Set prefix to Control + a (more ergonomic than C-b)
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# Start window and pane numbering at 1 (instead of 0)
set -g base-index 1
setw -g pane-base-index 1

# Automatically renumber windows when one is closed
set -g renumber-windows on

# Increase scrollback history limit
set -g history-limit 10000

# Enable mouse support
set -g mouse on

# Use vim keybindings in copy mode
setw -g mode-keys vi

# Copy and clipboard integration
set -s set-clipboard on
# Keep selection visible and copy it to the clipboard when drag ends
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe "wl-copy || xsel -ib"

# Fix escape time delay in Neovim
set -sg escape-time 0

# Set terminal overrides for true colors (24-bit color support)
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

# --- Keymaps ------------------------------------------------------------------
# Easy config reload
bind r source-file ~/.tmux.conf \; display-message "¡Configuración de tmux recargada!"

# Split panes using | and - (more intuitive)
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# Open new windows in the current path
bind c new-window -c "#{pane_current_path}"

# --- Vim-Tmux Navigator Integration ------------------------------------------
# Smart pane switching with awareness of Vim splits.
# Allows seamless navigation using Alt+h/j/k/l between Vim and Tmux
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?|fzf)(diff)?$'"
bind-key -n 'M-h' if-shell "$is_vim" 'send-keys M-h'  'select-pane -L'
bind-key -n 'M-j' if-shell "$is_vim" 'send-keys M-j'  'select-pane -D'
bind-key -n 'M-k' if-shell "$is_vim" 'send-keys M-k'  'select-pane -U'
bind-key -n 'M-l' if-shell "$is_vim" 'send-keys M-l'  'select-pane -R'

tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" -eq 1 ]' \
    "bind-key -n 'M-\\' if-shell \"$is_vim\" 'send-keys M-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" -eq 1 ]' \
    "bind-key -n 'M-\\' if-shell \"$is_vim\" 'send-keys M-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'M-h' select-pane -L
bind-key -T copy-mode-vi 'M-j' select-pane -D
bind-key -T copy-mode-vi 'M-k' select-pane -U
bind-key -T copy-mode-vi 'M-l' select-pane -R

# --- Visual Styling (Catppuccin Mocha Palette) --------------------------------
# Colors
# Base: #1e1e2e, Surface: #313244, Overlay: #6c7086, Text: #cdd6f4
# Blue: #89b4fa, Lavender: #b4befe, Sapphire: #74c7ec, Mauve: #cba6f7
# Green: #a6e3a1, Peach: #fab387, Red: #f38ba8

# Pane styling
set -g pane-border-style fg="#313244"
set -g pane-active-border-style fg="#b4befe" # Lavender for active pane

# Message styling
set -g message-style bg="#313244",fg="#cdd6f4"

# Status bar options
set -g status "on"
set -g status-justify "left"
set -g status-position "bottom"
set -g status-left-length "100"
set -g status-right-length "100"

# Status bar colors
set -g status-style bg="#1e1e2e",fg="#cdd6f4"

# Left side status: Session Name (styled capsule)
set -g status-left "#[fg=#1e1e2e,bg=#b4befe,bold]  #S #[fg=#b4befe,bg=#1e1e2e,nobold]"

# Right side status: Host name, Date & Time (capsule design)
set -g status-right "#[fg=#313244,bg=#1e1e2e]#[fg=#cdd6f4,bg=#313244]  #h #[fg=#89b4fa,bg=#313244]#[fg=#1e1e2e,bg=#89b4fa,bold]  %H:%M #[fg=#a6e3a1,bg=#89b4fa]#[fg=#1e1e2e,bg=#a6e3a1,bold]  %d-%b "

# Window Status Formatting
# Inactive windows
set -g window-status-format "#[fg=#6c7086,bg=#1e1e2e]  #I:#W  "

# Active window (highlighted pill/capsule)
set -g window-status-current-format "#[fg=#1e1e2e,bg=#cba6f7,bold] #I:#W "
